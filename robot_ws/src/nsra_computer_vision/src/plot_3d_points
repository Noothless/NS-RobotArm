#!/usr/bin/python
import numpy as np
import cv2
from mpl_toolkits import mplot3d
import matplotlib.pyplot as plt
import rospy
from std_msgs.msg import String
import rospkg

rospack = rospkg.RosPack()

fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')

fs = cv2.FileStorage(rospack.get_path('nsra_computer_vision') + "/storedPoints.yml", cv2.FILE_STORAGE_WRITE)

fn = fs.getNode("Points")

flag = False

x=fn[:,0]
y=fn[:,1]
z=fn[:,2]

ax.scatter(x, y, z)
plt.show()
